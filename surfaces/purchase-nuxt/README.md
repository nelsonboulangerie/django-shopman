# purchase-nuxt

Nuxt operator surface for Shopman Buyman.

This surface consumes the Backstage projection at `/api/v1/backstage/buyman/`
and writes through the same BFF proxy pattern used by the other operator apps.
The canonical Buyman/Stockman/Craftsman packages stay agnostic; orchestration
lives in Backstage.

## Product notes

- Primary navigation: `Painel`, `Comprar`, `Receber`, `Base`.
- `Painel` is the decision queue for the purchasing operator.
- `Comprar` handles replenishment and purchase requests generated by stock, production, or other teams.
- `Receber` is tablet/mobile-first and optimized for NF QR/barcode scanning, photo fallback, manual receipt exceptions, quantity checks, conversions, validity, stock entry confirmation, and refusal/return registration.
- `Base` exposes operational references that support the flows: materials, suppliers, costs, and conversions.

TODO: review the other Nuxt operator surfaces and define a project-wide pattern
for what remains in Django Admin/Unfold versus what belongs in Nuxt because it
is part of a dynamic operational workflow.

## BFF contract

The read side is centralized in `app/composables/usePurchaseDesk.ts` with
`useFetch`. Writes are centralized in `app/composables/usePurchaseApi.ts`.
If the API is unavailable during isolated frontend development, the local demo
dataset remains as read-only fallback. Real operation depends on the Backstage
API; write actions are blocked outside an authenticated backend session.

Endpoints:

- `GET /api/v1/backstage/buyman/` -> `{ purchase }` projection.
- `POST /api/v1/backstage/buyman/receipts/scan-invoice/` -> validate QR/barcode/chave NF and create a receipt draft.
- `POST /api/v1/backstage/buyman/receipts/confirm/` -> confirm stock entry with conversions, validity, costs, and BUY ledger moves.
- `POST /api/v1/backstage/buyman/receipts/reject/` -> record a supplier delivery refusal/return as an internal `notification.send`, without stock movement.
- `POST /api/v1/backstage/buyman/requests/<material_sku>/approve/` -> approve a purchase request.
- `POST /api/v1/backstage/buyman/requests/<material_sku>/send/` -> queue `notification.send` to the supplier channel and mark the request as sent.
- `POST /api/v1/backstage/buyman/costs/` -> record an observed cost/conversion from an operational flow.

Supplier dispatch uses Buyman data without extending Core: `Supplier.email`,
`Supplier.phone`, or `Supplier.metadata.purchase.contact`. A supplier can set
`metadata.purchase.order_channel` to `email`, `sms`, `whatsapp`, `manychat`, or
`console`; otherwise Compras prefers e-mail, then phone/SMS.

```bash
npm install
npm run dev
```
