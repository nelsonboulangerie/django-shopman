__pycache__/
*.py[cod]
*.egg-info/
*.egg
dist/
build/
.eggs/
*.so
.env
db.sqlite3
dump.rdb
.pytest_cache/
.ruff_cache/
.coverage
htmlcov/
*.log
.DS_Store
.venv/
venv/
.claude/
.codex/
.secrets/
node_modules/
shopman/static/storefront/css/output.css
staticfiles/*
!staticfiles/.gitkeep
.tools/

# Local screen recordings (never commit)
*.MP4
*.mp4

# Local scratch/temp files (never commit)
temp/

# Playwright
surfaces/storefront-nuxt/test-results/
surfaces/storefront-nuxt/playwright-report/
surfaces/storefront-nuxt/blob-report/

# Quarentena local e guias de credencial — nunca commitar.
# ⚠️ LIMPAR ANTES DO GO-LIVE: estes caminhos existem na máquina do Pablo e
# devem sair de vez (o guia de credencial não pode ficar no disco do projeto).
# Ver docs/plans/GO-LIVE-READINESS-PLAN.md.
_to_delete/
guia-credenciais-*.pdf

# Dados externos locais (ex.: export Yooga) — nunca commitar
var/

# Credenciais — o repo é PÚBLICO. Reconstruído a partir do
# shopman/shop/tests/test_secret_files_never_tracked.py, que sobreviveu ao
# `git reset --hard` que apagou a versão original desta seção.
*.pem
*.p12
*.pfx
*.key
*.jks
*.keystore
*.crt
*.cer
.env.*
!.env.example
